# pytest.ini
[pytest]
DJANGO_SETTINGS_MODULE = core.settings_test
pythonpath = . sync

addopts =
    -v
    --tb=short
    --strict-markers
markers =
    slow: marks tests as slow (deselect with -m "not slow")